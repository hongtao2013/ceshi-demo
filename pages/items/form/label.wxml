<view class="form-label-container view-container">
	<view class="view-title">label 表单组件</view>

	
	<view class="view-attr">
		<view decode="true">
			用来改进表单组件的可用性，使用for属性找到对应的id，或者将控件放在该标签下，当点击时，就会触发对应的控件。for优先级高于内部控件，内部有多个控件的时候默认触发第一个控件。目前可以绑定的控件有：&lt;button/&gt;, &lt;checkbox/&gt;, &lt;radio/&gt;, &lt;switch/&gt;。
		</view>

	</view>
</view>